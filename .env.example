# .env.example (COMPLETO)
# =======================================================
# Variáveis do Projeto (Padrão)
# =======================================================
NODE_ENV=development  # ← AGORA no .env também!
PORT=3000

# =======================================================
# CONFIGURAÇÃO DO BANCO DE DADOS (POSTGRESQL)
# =======================================================
DB_HOST=database
DB_USER=dev
DB_PASSWORD=dev123
DB_NAME=myapp
DB_PORT=5432

# =======================================================
# CONFIGURAÇÃO DO JWT (JSON WEB TOKEN)
# =======================================================
JWT_SECRET=REPLACE_WITH_RANDOM_64_CHAR_SECRET_FOR_EACH_PROJECT
JWT_EXPIRES_IN=7d

# =======================================================
# PGADMIN (OPCIONAL)
# =======================================================
PGADMIN_EMAIL=admin@app.com
PGADMIN_PASSWORD=admin123

# Cria um arquivo temporário com a key
#openssl rand -base64 64 > temp_key.txt
#cat temp_key.txt
#rm temp_key.txt